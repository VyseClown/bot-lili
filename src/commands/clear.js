module.exports = {
    name: "clear",
    description: "Para limpar mensagens em um chat",
    category: "staff",
    timeout: 3000,
    aliases: ["limpar"],

    async execute(client, message, args) {
    }
}